// ConsoleAppCmake.h: arquivo de inclusão para arquivos de inclusão padrão do sistema,
// ou arquivos de inclusão específicos a um projeto.

#pragma once

#include <iostream>

float celsiusToFarenheit(float celsius);
float farenheitToCelsius(float farenheit);
float multiply(float x, float y);
float divide(float x, float y);
float square(float x);
float cube(float x);

// TODO: Referencie os cabeçalhos adicionais de que seu programa precisa aqui.
